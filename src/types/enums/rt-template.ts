/**
 * Recurrent Transaction Template
 *
 * As the recurrent transactions only can be created
 * using templates, we need this enum to allow the user
 * to communicate with the backend
 */
export enum RtTemplateEnum {
	"SALARY_DEFAULT" = "SALARY_DEFAULT",
}
