type: object
title: CardEntity
required:
  - id
  - accountId
  - cardProviderId
  - name
  - lastFourDigits
properties:
  id:
    description: Card ID
    type: string
    format: uuid
  accountId:
    description: User ID
    type: string
    format: uuid
  cardProviderId:
    description: Card provider ID
    type: string
    format: uuid
  name:
    $ref: ../fields/name.yaml
  lastFourDigits:
    $ref: ../fields/last-four-digits.yaml
  dueDay:
    $ref: ../fields/due-day.yaml
  limit:
    $ref: ../fields/card-limit.yaml
  balance:
    $ref: ../fields/balance.yaml
