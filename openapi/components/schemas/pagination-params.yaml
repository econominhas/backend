- in: query
  name: page
  schema:
    type: integer
    default: 1
  description: Page for the pagination
- in: query
  name: limit
  schema:
    type: integer
    default: 10
  description: Limit of items to bring per page
#
# -------- ALERT --------
# As openapi syntax doesn't support extending this to add
# more params, there are some routes that require to use the
# params hardcoded instead of using this ref, so if you are
# going to change this, please make sure to update all
# the other routes as well
# -----------------------
