type: object
title: BankAccountEntity
required:
  - id
  - accountId
  - bankId
  - accountNumber
  - branch
  - balance
  - name
properties:
  id:
    description: Bank account ID
    type: string
    format: uuid
  accountId:
    description: User ID
    type: string
    format: uuid
  bankId:
    description: Bank provider ID
    type: string
    format: uuid
  accountNumber:
    $ref: ../fields/account-number.yaml
  branch:
    $ref: ../fields/branch.yaml
  balance:
    $ref: ../fields/balance.yaml
  name:
    $ref: ../fields/name.yaml
